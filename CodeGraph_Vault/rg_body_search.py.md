---
type: codegraph-node
kind: file
file: "scripts/rg_body_search.py"
---

# rg_body_search.py

**位置:** `scripts/rg_body_search.py`
**类别:** `file`

---

### 🧩 核心下游调用
- ➡️ 调用 [[os]] *(类型: contains)*
- ➡️ 调用 [[re]] *(类型: contains)*
- ➡️ 调用 [[sys]] *(类型: contains)*
- ➡️ 调用 [[_SCRIPTS_DIR]] *(类型: contains)*
- ➡️ 调用 [[utils]] *(类型: contains)*
- ➡️ 调用 [[FRONTMATTER_RE]] *(类型: contains)*
- ➡️ 调用 [[search_body]] *(类型: contains)*
- ➡️ 调用 [[expand_paths]] *(类型: contains)*
- ➡️ 调用 [[main]] *(类型: contains)*
- ➡️ 调用 [[utils]] *(类型: imports)*
- ➡️ 调用 [[main]] *(类型: calls)*

