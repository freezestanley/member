---
type: codegraph-node
kind: file
file: "scripts/memory_manager.py"
---

# memory_manager.py

**位置:** `scripts/memory_manager.py`
**类别:** `file`

---

### 🧩 核心下游调用
- ➡️ 调用 [[os]] *(类型: contains)*
- ➡️ 调用 [[re]] *(类型: contains)*
- ➡️ 调用 [[math]] *(类型: contains)*
- ➡️ 调用 [[datetime]] *(类型: contains)*
- ➡️ 调用 [[BRAIN_DIR]] *(类型: contains)*
- ➡️ 调用 [[GLOBAL_DIR]] *(类型: contains)*
- ➡️ 调用 [[PROJECT_DIR]] *(类型: contains)*
- ➡️ 调用 [[ARCHIVE_DIR]] *(类型: contains)*
- ➡️ 调用 [[HALF_LIFE_DAYS]] *(类型: contains)*
- ➡️ 调用 [[FORGET_THRESHOLD]] *(类型: contains)*
- ➡️ 调用 [[_GENERATED_FILES]] *(类型: contains)*
- ➡️ 调用 [[calculate_weight]] *(类型: contains)*
- ➡️ 调用 [[_build_frontmatter]] *(类型: contains)*
- ➡️ 调用 [[_ensure_status_fields]] *(类型: contains)*
- ➡️ 调用 [[archive_with_backlink_update]] *(类型: contains)*
- ➡️ 调用 [[scan_and_clean]] *(类型: contains)*
- ➡️ 调用 [[render_global_indices]] *(类型: contains)*
- ➡️ 调用 [[datetime]] *(类型: imports)*
- ➡️ 调用 [[scan_and_clean]] *(类型: calls)*
- ➡️ 调用 [[render_global_indices]] *(类型: calls)*

