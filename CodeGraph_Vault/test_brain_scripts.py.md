---
type: codegraph-node
kind: file
file: "tests/test_brain_scripts.py"
---

# test_brain_scripts.py

**位置:** `tests/test_brain_scripts.py`
**类别:** `file`

---

### 🧩 核心下游调用
- ➡️ 调用 [[sys]] *(类型: contains)*
- ➡️ 调用 [[math]] *(类型: contains)*
- ➡️ 调用 [[memory_manager]] *(类型: contains)*
- ➡️ 调用 [[test_cold_start_access_count_1]] *(类型: contains)*
- ➡️ 调用 [[test_cold_start_access_count_0_no_crash]] *(类型: contains)*
- ➡️ 调用 [[test_frequency_bonus_increases_with_access]] *(类型: contains)*
- ➡️ 调用 [[subprocess]] *(类型: contains)*
- ➡️ 调用 [[os]] *(类型: contains)*
- ➡️ 调用 [[time]] *(类型: contains)*
- ➡️ 调用 [[pathlib]] *(类型: contains)*
- ➡️ 调用 [[test_archive_preserves_path_and_status]] *(类型: contains)*
- ➡️ 调用 [[test_archive_updates_backlinks_cross_dir]] *(类型: contains)*
- ➡️ 调用 [[test_vault_sync_skips_when_lock_exists]] *(类型: contains)*
- ➡️ 调用 [[test_rg_body_search_excludes_frontmatter]] *(类型: contains)*
- ➡️ 调用 [[test_rg_body_search_hits_body]] *(类型: contains)*
- ➡️ 调用 [[pickle]] *(类型: contains)*
- ➡️ 调用 [[time]] *(类型: contains)*
- ➡️ 调用 [[test_bm25_cache_created_on_first_run]] *(类型: contains)*
- ➡️ 调用 [[test_bm25_cache_not_rebuilt_when_unchanged]] *(类型: contains)*
- ➡️ 调用 [[test_rg_body_search_hits_alias]] *(类型: contains)*
- ➡️ 调用 [[test_rg_body_search_alias_with_comment]] *(类型: contains)*
- ➡️ 调用 [[test_bm25_aliases_increase_score]] *(类型: contains)*
- ➡️ 调用 [[test_clean_tokenize_strips_frontmatter]] *(类型: contains)*
- ➡️ 调用 [[test_clean_tokenize_strips_html_comment]] *(类型: contains)*
- ➡️ 调用 [[test_clean_tokenize_body_separator_not_eaten]] *(类型: contains)*
- ➡️ 调用 [[test_extract_outline_h1_h2_h3]] *(类型: contains)*
- ➡️ 调用 [[test_extract_outline_skips_frontmatter_headings]] *(类型: contains)*
- ➡️ 调用 [[test_render_hot_outline_format]] *(类型: contains)*
- ➡️ 调用 [[test_render_hot_empty_outline]] *(类型: contains)*
- ➡️ 调用 [[test_lock_is_held_detects_real_lock]] *(类型: contains)*
- ➡️ 调用 [[test_lock_is_held_free_when_no_holder]] *(类型: contains)*
- ➡️ 调用 [[test_lock_is_held_missing_file]] *(类型: contains)*
- ➡️ 调用 [[subprocess]] *(类型: contains)*
- ➡️ 调用 [[tempfile]] *(类型: contains)*
- ➡️ 调用 [[shutil]] *(类型: contains)*
- ➡️ 调用 [[pathlib]] *(类型: contains)*
- ➡️ 调用 [[REPO_ROOT]] *(类型: contains)*
- ➡️ 调用 [[SCRIPT]] *(类型: contains)*
- ➡️ 调用 [[_make_wiki]] *(类型: contains)*
- ➡️ 调用 [[test_global_route_writes_global_hot]] *(类型: contains)*
- ➡️ 调用 [[test_project_route_writes_project_hot]] *(类型: contains)*
- ➡️ 调用 [[test_global_top_n_capped_at_8]] *(类型: contains)*
- ➡️ 调用 [[test_empty_project_generates_skeleton]] *(类型: contains)*
- ➡️ 调用 [[test_watcher_script_syntax]] *(类型: contains)*
- ➡️ 调用 [[test_old_hot_md_not_referenced_in_claude_md]] *(类型: contains)*
- ➡️ 调用 [[test_no_project_notes_in_global_hot]] *(类型: contains)*
- ➡️ 调用 [[memory_manager]] *(类型: imports)*
- ➡️ 调用 [[pathlib]] *(类型: imports)*

