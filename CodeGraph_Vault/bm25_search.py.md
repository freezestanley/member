---
type: codegraph-node
kind: file
file: "scripts/bm25_search.py"
---

# bm25_search.py

**位置:** `scripts/bm25_search.py`
**类别:** `file`

---

### 🧩 核心下游调用
- ➡️ 调用 [[os]] *(类型: contains)*
- ➡️ 调用 [[re]] *(类型: contains)*
- ➡️ 调用 [[sys]] *(类型: contains)*
- ➡️ 调用 [[pickle]] *(类型: contains)*
- ➡️ 调用 [[_SCRIPTS_DIR]] *(类型: contains)*
- ➡️ 调用 [[jieba]] *(类型: contains)*
- ➡️ 调用 [[rank_bm25]] *(类型: contains)*
- ➡️ 调用 [[utils]] *(类型: contains)*
- ➡️ 调用 [[BRAIN_DIR]] *(类型: contains)*
- ➡️ 调用 [[GLOBAL_DIR]] *(类型: contains)*
- ➡️ 调用 [[PROJECT_DIR]] *(类型: contains)*
- ➡️ 调用 [[CACHE_PATH]] *(类型: contains)*
- ➡️ 调用 [[clean_and_tokenize]] *(类型: contains)*
- ➡️ 调用 [[collect_md_paths]] *(类型: contains)*
- ➡️ 调用 [[build_or_load_cache]] *(类型: contains)*
- ➡️ 调用 [[main]] *(类型: contains)*
- ➡️ 调用 [[rank_bm25]] *(类型: imports)*
- ➡️ 调用 [[utils]] *(类型: imports)*
- ➡️ 调用 [[main]] *(类型: calls)*

